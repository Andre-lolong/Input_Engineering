# ask the user for their fullname
fullname = input("Enter your fullname: ")

count = len(fullname) # count the characters

# print how many characters are in the user's fullname
print(f"The character count in your input is: {count} ")